# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Deepwell generated data (rebuilt by the pipeline)
data/extracted/
data/chunks/
data/enriched/
data/vectorstore/
data/knowledge.db
data/sources/zim/.catalog.json
data/sources/zim/.manifest.json

# Large source archives (ZIMs are hundreds of MB+)
data/sources/zim/*.zim

# Source documents (kept locally, not in repo)
data/sources/pdf/*.pdf

# Locally-rendered web article snapshots (regenerated by the ingest pipeline)
data/sources/web/

# OS noise
.DS_Store

# Local secrets / runtime config
.env
